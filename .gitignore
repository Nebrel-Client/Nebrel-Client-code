# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.idea
examples
# Renamed artefacts staged for a GitHub release upload.
release-upload/

# Updater signing key. The private half must never be committed.
nebrel.key
nebrel.key.pub
*.key

# Deployment archives, rebuilt on demand rather than versioned.
nebrel-backend-update.zip
nebrel-backend-server-update.tar.gz
nebrel-backend-update/

# Sample feed staged for upload to the news host.
news-site/

# Deployment archive, rebuilt from the backend sources on demand.
nebrel-hosting-backend-update.tar.gz
